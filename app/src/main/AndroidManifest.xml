<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.itchihuahuaii.tutoriales">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ejemploWebView"
            android:configChanges="orientation|keyboardHidden"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ejemploSpinner" />
        <activity android:name=".ejemploScroll" />
        <activity android:name=".ejemploDialog" />
        <activity android:name=".ejemploDialogImage" />
        <activity android:name=".ejemploAutoComplete" />
        <activity android:name=".ejemploListView" />
        <activity android:name=".listViewCustom" />
        <activity android:name=".ejemploTableLayout" />
        <activity android:name=".ejemploMusic" />
        <activity android:name=".ejemploLogin2" />
        <activity android:name=".ejemploFrameLayout" />
        <activity android:name=".ejemploLogin" />
        <activity android:name=".ejemploBundle"></activity>
    </application>

</manifest>